import React from 'react';
import { Table } from 'react-bootstrap';
import { getRemainingDaysBadge } from '../utils/utils';

const CoursesTable = ({ courses, withExpiry }) => {
  return (
    <Table borderless size="sm">
      
    </Table>
  );
};

export default CoursesTable;
